/** App stays intentionally tiny: page composition belongs in the pages/components folders. */
import { GalleryPage } from "./pages/GalleryPage";
import "./styles.css";

export default function App() { return <GalleryPage />; }
